{
  "manifest_version": "0.3",
  "name": "bug-tracker",
  "version": "1.0.0",
  "description": "Logs bugs into the team's internal bug tracker via the log_bug tool.",
  "author": {
    "name": "your-name-here"
  },
  "server": {
    "type": "node",
    "entry_point": "index.js",
    "mcp_config": {
      "command": "node",
      "args": ["${__dirname}/index.js"],
      "env": {
        "BUG_TRACKER_URL": "${user_config.bug_tracker_url}",
        "AGENT_TOKEN": "${user_config.agent_token}"
      }
    }
  },
  "user_config": {
    "bug_tracker_url": {
      "type": "string",
      "title": "Bug Tracker URL",
      "description": "The address of your locally running bug tracker.",
      "default": "http://localhost:3000",
      "required": true
    },
    "agent_token": {
      "type": "string",
      "title": "Agent Token (optional)",
      "description": "Only fill this in if you set AGENT_TOKEN in the tracker's .env file. Leave blank otherwise.",
      "sensitive": true,
      "required": false
    }
  }
}
